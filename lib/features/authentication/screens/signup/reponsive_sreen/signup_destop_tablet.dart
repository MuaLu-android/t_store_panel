import 'package:flutter/material.dart';

class SignupDestopTablet extends StatelessWidget {
  const SignupDestopTablet({super.key});

  @override
  Widget build(BuildContext context) {
    // implement build
    throw UnimplementedError();
  }
}
