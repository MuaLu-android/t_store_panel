import 'package:flutter/material.dart';

class EditBrandsTablets extends StatelessWidget {
  const EditBrandsTablets({super.key});

  @override
  Widget build(BuildContext context) {
    // implement build
    throw UnimplementedError();
  }
}
