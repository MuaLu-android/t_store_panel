import 'package:flutter/material.dart';

class EditBrandsMobile extends StatelessWidget {
  const EditBrandsMobile({super.key});

  @override
  Widget build(BuildContext context) {
    // implement build
    throw UnimplementedError();
  }
}
