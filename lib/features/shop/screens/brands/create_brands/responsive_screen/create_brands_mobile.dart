import 'package:flutter/material.dart';

class CreateBrandsMobile extends StatelessWidget {
  const CreateBrandsMobile({super.key});

  @override
  Widget build(BuildContext context) {
    // implement build
    throw UnimplementedError();
  }
}
