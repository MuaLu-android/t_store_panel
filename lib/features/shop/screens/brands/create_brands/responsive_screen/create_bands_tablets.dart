import 'package:flutter/material.dart';

class CreateBandsTablets extends StatelessWidget {
  const CreateBandsTablets({super.key});

  @override
  Widget build(BuildContext context) {
    // implement build
    throw UnimplementedError();
  }
}
