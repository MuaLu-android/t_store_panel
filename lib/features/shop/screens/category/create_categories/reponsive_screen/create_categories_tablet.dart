import 'package:flutter/material.dart';

class CreateCategoriesTablet extends StatelessWidget {
  const CreateCategoriesTablet({super.key});

  @override
  Widget build(BuildContext context) {
    // implement build
    throw UnimplementedError();
  }
}
